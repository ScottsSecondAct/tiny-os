[workspace]
resolver = "2"
members = ["kernel", "arch", "bsp"]

[profile.dev]
panic = "abort"
opt-level = 1

[profile.release]
panic = "abort"
opt-level = 3
lto = "fat"
codegen-units = 1
strip = false
